import { SvgIcon } from '@mui/material';

export const MilkIcon = (props) => (
  <SvgIcon {...props}>
    {/* Custom SVG path for milk icon */}
  </SvgIcon>
);

export const EggIcon = (props) => (
  <SvgIcon {...props}>
    {/* Custom SVG path for egg icon */}
  </SvgIcon>
);

// Add other custom icons... 